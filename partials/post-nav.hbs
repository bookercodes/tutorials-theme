<nav class="site-nav">
    <div class="site-nav-left post-nav-left">
        {{^is "home"}}
            {{#if @site.logo}}
                <a class="site-nav-logo" href="https://cometchat.com/"><img src="{{@site.logo}}" alt="{{@site.title}}" /></a>
            {{else}}
                <a class="site-nav-logo" href="https://cometchat.com/">{{@site.title}}</a>
            {{/if}}
        {{/is}}
        {{#if @site.navigation}}
            {{navigation}}
        {{/if}}
        <a class="cta" href="https://app.cometchat.com/register" title="Use CometChat for free" target="_blank" rel="noopener">Use CometChat for free</a>
    </div>
</nav>
